package dk.digitalidentity.os2faktor.security;

// order 104 is before the SAML module but after the build-in adapter
//@Order(104)
//@Configuration
public class XFrameOptionsConfig { /*extends WebSecurityConfigurerAdapter {

	@Override
	protected void configure(HttpSecurity http) throws Exception {
		http.headers().frameOptions().disable();
	}
	*/
}
